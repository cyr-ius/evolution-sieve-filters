# User-visible strings of the Evolution module (menus, dialog, visual
# editor, account page) -- see POTFILES.in. `preset: 'glib'` sets the
# extraction keywords (_ N_ C_ NC_) for xgettext.
i18n.gettext(gettext_package, preset: 'glib')
